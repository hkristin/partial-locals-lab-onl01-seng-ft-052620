<%= form_tag students_path, method: :get do %>
  <p>
    <%= if text_field_tag.empty? %>
    <%= return @students %>
    <%= else text_field_tag :query, params[:query] %>
    <%= submit_tag "Search"%>
  </p>
<% end %>

<% @students.each do |student| %>
  <%= render partial: 'students/student', locals: {student: student} %>
<% end %>
